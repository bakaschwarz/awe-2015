%p#notice= notice
%h1 Gruppen Liste
%table
  %thead
    %tr
      %th Name
      %th{:colspan => "3"}
  %tbody
    - @groups.each do |group|
      %tr
        %td= group.name
        %td= link_to 'Anzeigen', group
        %td= link_to 'Bearbeiten', edit_group_path(group)
        %td= link_to 'Entfernen', group, method: :delete, data: { confirm: 'Sind Sie sich sicher?' }
%br/
= link_to 'Neue Gruppe', new_group_path