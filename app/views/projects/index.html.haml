%p#notice= notice
%h1 Listing Projects
%table
  %thead
    %tr
      %th Name
      %th Repo
      %th Group
      %th{:colspan => "3"}
  %tbody
    - @projects.each do |project|
      %tr
        %td= project.name
        %td= project.repo
        %td= project.group_id
        %td= link_to 'Anzeigen', project
        %td= link_to 'Bearbeiten', edit_project_path(project)
        %td= link_to 'Entfernen', project, method: :delete, data: { confirm: 'Sind Sie sich sicher?' }
%br/
= link_to 'Neues Projekt', new_project_path
