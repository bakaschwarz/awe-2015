= javascript_include_tag "//www.google.com/jsapi", "chartkick"
%h1 Line chart
-@sensors.each do |sensor|
  -hashed_data = Hash[ sensor.sensor_data.map{ |sd| [Time.at(sd.time_stamp).to_datetime,sd.value] } ]
  %h2
    = sensor.label
    = "["
    = sensor.unit
    = "]"
  = line_chart hashed_data